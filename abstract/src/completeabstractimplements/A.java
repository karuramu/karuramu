package completeabstractimplements;

public interface A {
public void test();

/*interfaces can store only incomplete methods 
 * by default every method is abstract in an interface 
 * every variable in an interface public static final
 * multiple inheritance supported in interface
 * 
 * abstract class is consists of both complete ,incomplete methods
 * every method by default is not abstract we should use abstract key word specific incomplete methods
 * variables by default is not final 
 * multiple inheritance in abstract class is not possible
 * inheritance b/w an interface and abstract class is possible 
 * 
 */
}
